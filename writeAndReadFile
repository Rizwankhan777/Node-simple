const fs =require('fs')

fs.writeFileSync('filler.txt','hello this new file in node');
fs.readFile('./filler.txt', 'utf8' , (err, data) => {
    if (err) throw err;
    console.log(data);
  });
  
console.log("created new file ");
